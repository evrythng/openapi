name: purchaseOrderId
in: path
description: The unique purchase order identifier. May also be the evrythngId from the purchase order.
example: '1234567890'
required: true
schema:
  type: string
  minLength: 1
  maxLength: 32
  pattern: '^[a-zA-Z0-9_+-.@]+$'
