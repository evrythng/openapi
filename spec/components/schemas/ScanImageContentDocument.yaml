additionalProperties: false
type: object
description: 'An object containing image data in Base 64 encoding.'
required:
  - image
properties:
  image:
    type: string
    description: 'Base 64 encoded image data'
